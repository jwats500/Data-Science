### To Deploy Model ###

1. Edit path variables in code block 2 (shown below). Note: Folder path needs to need with no '\' included in training_image_path. 

property_csv_path = ''
training_images_path = ''
model_outpath = ''

2. Run all code blocks. 